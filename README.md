# Pet_Care_Center
This project it's an SPA (Single Page Application) that register relevant information about its users, as clients and workers.  For this project, we used HTML, CSS, JavaScript and Json-server.
